import 'package:flutter/material.dart';

class Chip extends StatelessWidget {
  String url, title;
  Chip({this.url, this.title});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
